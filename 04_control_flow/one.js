// oprators in JS

// lessThan (<), greaterThan (>), lessThanEquals (<=),
// greaterThanEquals (>=), equal (==), notEquals (!=),
// identical (===), notIdentical (!==), 
// ternary operator (condition ? true : flase), 
// nullish coalescing operator (??) : null, undefind
// logicalAND (&&), logicalOR (||), logicalNOT (!)